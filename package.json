{
    "name": "visitlo-embed",
    "version": "1.0.0",
    "description": "A package for embedding Visitlo tours, products and shops on your website.",
    "main": "index.js",
    "scripts": {
        "build": "tsup src/index.ts --format cjs --dts-resolve --minify --clean --sourcemap --legacy-output --out-dir dist"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@changesets/cli": "^2.27.1",
        "tsup": "^8.0.1",
        "typescript": "^5.3.3"
    }
}
